@model List<TbCatCiudad>

<h1>Ciudad</h1>

<p>
    <a asp-action="Create" class="btn btn-success mb-3">Agregar Ciudad</a>
</p>

<!-- Tabla de Pais -->
<div class="table-responsive">
    <table class="table table-hover table-striped table-bordered">
        <thead class="table-primary">
            <tr>

                <th>Ciudad</th>
                <th>Acciones</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var tbCatCiudad in Model)
            {
                <tr>
                    <td>@Html.DisplayFor(m => tbCatCiudad.DescripcionCiudad)</td>
                    <td>
                        <!-- Botones para editar y eliminar -->
                        <a asp-action="Edit" asp-route-id="@tbCatCiudad.IdCiudad" class="btn btn-warning btn-sm">
                            <i class="bi bi-pencil"></i> Editar
                        </a>
                        <a asp-action="Delete" asp-route-id="@tbCatCiudad.IdCiudad" class="btn btn-danger btn-sm">
                            <i class="bi bi-trash"></i> Eliminar
                        </a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>
