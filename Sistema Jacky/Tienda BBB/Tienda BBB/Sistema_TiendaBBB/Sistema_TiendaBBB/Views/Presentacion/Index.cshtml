@model List<TbCatPresentacionArt>

<h1>Presentación</h1>

<p>
    <a asp-action="Create" class="btn btn-success mb-3">Agregar Presentación</a>
</p>

<!-- Tabla de Presentación -->
<div class="table-responsive">
    <table class="table table-hover table-striped table-bordered">
        <thead class="table-primary">
            <tr>

                <th>Presentación</th>
                <th>Acciones</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var tbCatPresentacionArt in Model)
            {
                <tr>
                    <td>@Html.DisplayFor(m => tbCatPresentacionArt.DescripcionPresentacion)</td>
                    <td>
                        <!-- Botones para editar y eliminar -->
                        <a asp-action="Edit" asp-route-id="@tbCatPresentacionArt.IdPresentacion" class="btn btn-warning btn-sm">
                            <i class="bi bi-pencil"></i> Editar
                        </a>
                        <a asp-action="Delete" asp-route-id="@tbCatPresentacionArt.IdPresentacion" class="btn btn-danger btn-sm">
                            <i class="bi bi-trash"></i> Eliminar
                        </a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>
