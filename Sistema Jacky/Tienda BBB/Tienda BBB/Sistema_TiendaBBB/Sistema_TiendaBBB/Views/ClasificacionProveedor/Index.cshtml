@model List<TbCatClasificacionProv>

<h1>Clasificación Proveedor</h1>

<p>
    <a asp-action="Create" class="btn btn-success mb-3">Agregar Clasificación</a>
</p>

<!-- Tabla de Clasificación -->
<div class="table-responsive">
    <table class="table table-hover table-striped table-bordered">
        <thead class="table-primary">
            <tr>

                <th>Clasificación</th>
                <th>Acciones</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var tbCatClasificacion in Model)
            {
                <tr>
                    <td>@Html.DisplayFor(m => tbCatClasificacion.DescripcionClasificacion)</td>
                    <td>
                        <!-- Botones para editar y eliminar -->
                        <a asp-action="Edit" asp-route-id="@tbCatClasificacion.IdClasificacion" class="btn btn-warning btn-sm">
                            <i class="bi bi-pencil"></i> Editar
                        </a>
                        <a asp-action="Delete" asp-route-id="@tbCatClasificacion.IdClasificacion" class="btn btn-danger btn-sm">
                            <i class="bi bi-trash"></i> Eliminar
                        </a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>